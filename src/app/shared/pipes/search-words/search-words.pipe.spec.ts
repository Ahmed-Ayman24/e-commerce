import { SearchWordsPipe } from './search-words.pipe';

describe('SearchWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
